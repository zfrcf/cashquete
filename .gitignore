# Xcode
*.xcodeproj
DerivedData/
xcuserdata/
*.xcuserstate

# SPM
.build/
.swiftpm/

# Node
node_modules/
# npm install lancé par erreur dans firebase/ (le vrai package.json est dans firebase/functions/)
firebase/package.json
firebase/package-lock.json

# Firebase
firebase/.firebase/
firebase-debug.log
.firebaserc

# Secrets — remplacer le placeholder puis NE PAS committer le vrai fichier
# CashQuest/GoogleService-Info.plist

# macOS / Windows
.DS_Store
Thumbs.db
